{
  "name": "fearless",
  "version": "1.0.0",
  "description": "This is an example of using 1-N relations",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "start:docker": "docker-compose pull && docker-compose up --remove-orphans",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Soramitsu",
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "@polkadot/api": "^7",
    "@subql/cli": "0.29.0",
    "@subql/types": "latest",
    "typescript": "4.3.2",
    "@equilab/definitions": "latest",
    "@subql/node": "0.35.2"
  }
}